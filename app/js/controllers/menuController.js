'use strict';

// $scope & $location are angular services, injected by name in controller
bugTrackerApp.controller('menuCtrl', function ($scope, $location) {

    $scope.routeIs = function (routeName) {
        // EXO-1.2.1 (compare actual route (see $location) with routeName)
    };

});
